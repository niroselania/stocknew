services:
  stock-buscador:
    build: .
    container_name: stock-buscador
    restart: unless-stopped
    ports:
      - "8088:8000"
    volumes:
      - stock-data:/data
    environment:
      HISTORY_MAX: "30"

volumes:
  stock-data:
